name: 'kairo'
version: '1.0.0'

profile: 'kairo'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  kairo:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table